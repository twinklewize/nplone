import 'package:flutter/material.dart';

class SharedZonePageWidget extends StatelessWidget {
  SharedZonePageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'page',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
