import 'package:flutter/material.dart';

class SharedZoneAccessWidget extends StatelessWidget {
  SharedZoneAccessWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'access',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
