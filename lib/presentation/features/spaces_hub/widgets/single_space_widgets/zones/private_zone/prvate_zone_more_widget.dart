import 'package:flutter/material.dart';

class PrivateZoneMoreWidget extends StatelessWidget {
  PrivateZoneMoreWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'more',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
